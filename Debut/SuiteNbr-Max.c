#include <stdio.h>
#include <stdlib.h>

int main()
{
    int nbr=0;

    while(nbr!=0) {
        printf("Rentrez un nombre",nbr);
        scanf("%d", &nbr);
    }
    printf("Votre nombre le plus grand est %d", &max)
}
